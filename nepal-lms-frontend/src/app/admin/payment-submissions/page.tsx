/* Administrator view of staff-submitted payments — most are already approved on capture; only flagged ones are still awaiting a second reviewer. */
export { default } from "@/app/staff/payment-submissions/page";
